/**
 * SentinelOps — Stores Barrel Export
 * ====================================
 * All global state stores are re-exported from this index for clean imports:
 *   import { useCameraStore } from "@/stores";
 *
 * This project uses React Context + useReducer for lightweight global state.
 * If complexity grows, consider migrating to Zustand.
 */

// Future stores will be exported here, e.g.:
// export { useCameraStore } from "./camera-store";
// export { useAlertStore } from "./alert-store";
// export { useThemeStore } from "./theme-store";
